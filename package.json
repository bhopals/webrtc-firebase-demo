{
  "name": "webrtc-demo",
  "version": "0.0.0",
  "homepage": "https://bhopals.github.io/webrtc-firebase-demo",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "vite": "^2.0.5"
  },
  "dependencies": {
    "firebase": "^8.2.10"
  }
}
